model_id: krutrim-2-12b
rank_by_bharat_score: 7
hf_checkpoint: Krutrim-AI-Labs/Krutrim-2-instruct
revision: null  # not commit-pinned - tracks the repo's mutable default branch
serving_backend: hf
apply_chat_template: true
device: cuda:1
dtype: bfloat16
quantization: none
batch_size: 8
generation_config:
  temperature: 0.0
  max_gen_toks: 512
mode: full
regimes: [R1, R2]
n_samples: 3
notes: >
  No per-model engineering note was recorded beyond backend/device/batch_size - it ran
  without needing a workaround. Not commit-pinned.
